# 9. Accessibility

- Node order defines semantic reading order.  
- Text and Label nodes MUST be exposed.  
- Interactive nodes (TextField) MUST support screen readers and input methods.  
- Backends MAY extend accessibility but MUST preserve core semantics.
